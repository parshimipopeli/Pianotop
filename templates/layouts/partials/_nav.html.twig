<header>
	<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
		<a class="navbar-brand" href="{{ path('app_home')}}">
			Pianotop
		</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarCollapse">
			<ul class="navbar-nav mr-auto"></ul>

			{% if app.user %}
				<a href="{{ path('app_account')}}" class="text-white text-decoration-none">
					Mon compte
				</a>
				<span class="text-white">
					|
				</span>

				<a href="{{ path('app_logout') }}" class="text-white text-decoration-none">
					Déconnexion
				</a>

			{% else %}
				<a href="{{ path('app_register')}}" class="text-white text-decoration-none">
					S'inscrire
				</a>
				<span class="text-white">
					|
				</span>

				<a href="{{ path('app_login') }}" class="text-white text-decoration-none">
					Connexion
				</a>


			{% endif %}
		</div>
	</nav>

</header>


