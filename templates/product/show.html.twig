{% extends "layouts/base.html.twig" %}

{% block title  %}
	{{ product.name }}
{% endblock %}


{% block body %}
	<div class="container">
		<div class="row mt-5">

			<div class="col-md-5 my-auto">
				<img src="/images/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid" height="100%" width="100%">
			</div>
			<div class="col-md-7 my-auto">
				<h3>
					{{ product.name }}
				</h3>
				<p class="text-muted">
					{{ product.subtitle}}
				</p>
				<span class="font-weight-bold">

					{{ (product.price / 100)|number_format(2, ',') }}
					€
				</span>
				<hr>
				<p>
					{{ product.description }}
				</p>
				<a href="" class="btn btn-primary">
					Ajouter au panier
				</a>
			</div>

		</div>
	</div>


{% endblock %}
